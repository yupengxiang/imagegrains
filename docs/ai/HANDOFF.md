# 当前交接：竞赛技术报告定稿阶段（2026-08-25）

## 当前目标

以现有代码、三张真实自然堆积样本及已有运行记录完成“混凝土骨料颗粒智能筛分比拼”自然堆积赛道的正式技术报告和后续答辩材料。

当前阶段不再安排新的机械筛分、人工实例标注或现场采样实验。报告必须基于现有证据完成闭环，不保留“待补真值”“后续回填实验表”等中期稿叙事。

## 唯一论文入口

- 正式报告：`docs/paper/main.tex`
- `docs/paper/main_submission.tex`：已删除
- `docs/paper/main_v2.tex`：已删除
- CI：`.github/workflows/paper-build.yml` 只编译 `docs/paper/main.tex`
- PDF artifact：`technical-report-pdf`

正文由 `docs/paper/reviewer/` 下的 7 个章节、参考文献和附录组成。

## 当前论文结构

1. 问题定义与主要贡献
2. 总体框架与技术路线
3. 视觉感知与物理测量
4. 从视觉颗粒到筛分级配
5. 实验结果与分析
6. 工程部署、质量控制与方法边界
7. 结论
8. 参考文献
9. 附录：关键参数与输出说明

正文目标是正式竞赛技术论文，而不是开发日志或待补实验的阶段报告。

## 当前证据集

三张真实自然堆积样本：

- `agg_001.JPG`
- `agg_005.JPG`
- `agg_029.JPG`

三张图共 3858 个实例。当前演示尺度约 0.208 mm/px，统一使用：

- `theta=(1,0,0)`：短轴作为筛分等效粒径基线；
- `gamma=3`：`w=d^gamma` 质量代理先验。

正常实例口径下的质量代理结果：

- `agg_001`: D10=11.1, D50=23.6, D90=30.6 mm；
- `agg_005`: D10=15.6, D50=27.5, D90=38.0 mm；
- `agg_029`: D10=6.7, D50=14.6, D90=30.6 mm。

同一实例集合的数量型 vs 质量代理型 D50：

- `agg_001`: 6.0 -> 23.5 mm；
- `agg_005`: 7.6 -> 27.5 mm；
- `agg_029`: 5.2 -> 14.1 mm。

竞赛工程层合成测试：27 passed。

已有算法阶段记录约 27 s/张（约 17 s 实例分割 + 9 s 几何测量 + <1 s 筛分/报告），只作为计算阶段时序，不写成完整 30 min SOP 的实测证明。

## 证据边界

当前报告不具备：

- 与三张图配对的机械筛分逐筛称量；
- 人工实例 mask；
- 三维针片状量规标签；
- 泥团/材料语义阳性标签。

因此全文统一采用以下术语：

- `视觉质量代理级配`，不写“标准筛分准确率”；
- `投影形貌`，不写“三维针片状真值”；
- `异常候选`，不写“泥团识别准确率”；
- 27 项测试用于说明实现一致性，不作为现实精度证据。

## 复用与项目贡献

复用：

- ImageGrains 2.0 / Cellpose-SAM 预训练颗粒实例分割模型。

本项目贡献：

1. 将预训练实例结果接入毫米级物理测量；
2. 建立显式的筛分等效粒径和质量代理统计；
3. 集成粒径、投影形貌、异常候选、结果报告和现场质检；
4. 保留逐颗粒到场景级输出的追溯关系。

不要把 ImageGrains 2.0 或 Cellpose-SAM 表述为本项目提出的方法。

## 写作规范

论文修改前读取：

`docs/ai/report-writing-skill/SKILL.md`

该 skill 的核心要求：

- 先写技术事实和机制，减少“值得注意”“需要指出”“这里强调”等回答者口吻；
- 减少反复的“不是 X / 不能说明 Y”防御性句式，证据边界集中表达；
- 结果段按“观察 -> 数据 -> 机制 -> 含义”组织；
- 方法段按“动机 -> 定义 -> 公式 -> 参数含义 -> 后果”组织；
- 保持术语、公式、数字、引用和 LaTeX label 稳定；
- 不为了所谓“去 AI”改变事实、增加口语错误或制造未验证结论。

## 当前写作状态

已完成两轮精修，正式报告进入定稿状态。

第一轮主要完成全文叙事重构、证据边界统一和章节语言优化；第二轮转向结构性压缩与图表审计，重点完成：

1. 删除第 3 节与部署章节重复的采集/成像控制内容；
2. 将第 4 节收紧为“筛分等效粒径 -> 质量代理 -> 加权分位数 -> 筛级聚合”的连续主线，删除重复公式和参数表；
3. 第 5 节直接从真实样本结果进入，明确数量型与质量代理型统计使用同一全部实例集合，并增加 `Delta D50` 对照；
4. 合并形貌与异常辅助结果表，减少重复证据说明；
5. 删除第 6 节与附录重复的结果包表，固定硬件、30 min SOP、失败恢复和方法边界表的章节归属；
6. 清理附录中的编辑过程语言；
7. 将结论独立起页，消除章节页眉与上一节共享页面造成的归属错觉。

第二轮最终正文构建为 17 页。最终 PDF 已逐页渲染检查，章节、图表、参考文献和附录的分页关系正常。

除事实纠错、赛题要求变化或答辩材料需要联动外，不建议继续对正文做大范围改写。下一阶段优先制作 PPT / 答辩材料，并从论文中抽取“问题 -> 技术选择 -> 筛分语义映射 -> 结果证据 -> 工程部署 -> 边界”的主线。

## 最终构建状态

GitHub Actions `Paper Build` 已确认：

- 17 页；
- XeLaTeX/BibTeX 编译成功；
- 无 unresolved reference/citation；
- 无 Overfull box；
- artifact 名称为 `technical-report-pdf`。

最终分页版本对应正文提交 `72ec2665`；之后的 README/HANDOFF 更新不改变论文内容。
